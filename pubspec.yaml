name: quiz_program
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  hive_ce: ^2.2.1
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15

dev_dependencies:
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
